### Estrategia de Gestión de Estado y Autenticación

- **Estado:** Aceptado.
- **Contexto:** Se requiere proteger rutas y mantener la sesión activa al recargar.
- **Decisión:** Implementar un **AuthContext** utilizando `useState` para el estado global y `localStorage` para la persistencia del token/flag de sesión.
- **Justificación:** Un Context Provider permite que el hook `useAuth` acceda al estado de sesión desde cualquier punto del árbol de componentes sin "prop drilling".

### Implementación

- **Archivo:** `src/contexts/AuthContext.tsx`
- **Hook:** `src/hooks/useAuth.ts`
- **Componente de protección:** `src/components/ProtectedRoute.tsx`
- **Rutas protegidas:** `/admin/turns`, `/admin/reservations`
- **Ruta pública de login:** `/login`